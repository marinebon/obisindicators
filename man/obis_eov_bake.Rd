% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eov.R
\name{obis_eov_bake}
\alias{obis_eov_bake}
\title{Bake the EOV membership and precomputed-indicator layers into a store}
\usage{
obis_eov_bake(con, eov = NULL, esn = 50L, verbose = TRUE)
}
\arguments{
\item{con}{a \code{DBI} connection to a \strong{writable} store with \code{occ_h3} + \code{taxon},
with the duckdb \code{h3} extension loaded.}

\item{eov}{EOV name(s) to bake; default all.}

\item{esn}{expected sample size for ES(n); default 50.}

\item{verbose}{message progress.}
}
\value{
invisibly, a data frame of \code{eov} and its member-taxon count.
}
\description{
Adds two tables to an obis_h3 DuckDB store:
\itemize{
\item \code{eov} — \verb{(eov, taxonID)} membership, each EOV's seeds expanded to their
full descendant set over the baked WoRMS \code{taxon} table.
\item \code{idx_h3_eov} — precomputed indicators per \verb{(eov, res)} for res 1-7, so an
EOV tile map reads a small clustered lookup instead of re-aggregating
\code{occ_h3} on every tile.
}
}
\details{
Requires \code{taxon} (see \code{data-raw/migrate_add_taxon.R}) and, for complete
coverage, a closed taxon tree (see \code{\link[=obis_taxon_fill_gaps]{obis_taxon_fill_gaps()}}) — any EOV
member whose AphiaID is missing from \code{taxon} is silently excluded.
}
\concept{eov}
