% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal.R
\name{calc_period_indicators}
\alias{calc_period_indicators}
\title{Indicators per period (e.g. decade) and cell}
\usage{
calc_period_indicators(
  con,
  res = 2L,
  eov = NULL,
  aphiaid = NULL,
  taxon = NULL,
  starts = seq(1960L, 2020L, by = 10L),
  width = 10L,
  esn = 50L
)
}
\arguments{
\item{con}{connection from \code{\link[=obis_store_connect]{obis_store_connect()}}.}

\item{res}{H3 resolution (coarse, e.g. 2-3, is sensible: the denominator
thins quickly when records are split by decade).}

\item{eov}{optional EOV name (one); see \code{\link[=obis_eov_seeds]{obis_eov_seeds()}}.}

\item{aphiaid}{optional WoRMS AphiaID(s) — the subtree filter.}

\item{taxon}{optional named list of DwC rank values; see \code{\link[=obis_h3t_sql]{obis_h3t_sql()}}.}

\item{starts}{period start years (default decades 1960-2020).}

\item{width}{period width in years (default 10).}

\item{esn}{expected sample size for ES(n); default 50.}
}
\value{
data frame with \code{period}, \code{cell}, \code{n}, \code{sp}, \code{es}.
}
\description{
Runs the year-filtered live tile SQL once per period and returns \code{n}, \code{sp}
and \code{es} per cell and period. Periods are \verb{[start, start + width - 1]}.
}
\concept{analyze}
