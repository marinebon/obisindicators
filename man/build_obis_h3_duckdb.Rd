% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/h3t.R
\name{build_obis_h3_duckdb}
\alias{build_obis_h3_duckdb}
\title{Build the OBIS H3 DuckDB store}
\usage{
build_obis_h3_duckdb(
  src,
  path_duckdb,
  region_bbox = NULL,
  esn = 50L,
  s3_region = "us-east-1",
  s3_anonymous = TRUE,
  overwrite = TRUE
)
}
\arguments{
\item{src}{occurrence source. Either a \code{data.frame} of occurrences, or a
character vector of parquet path(s)/glob(s) readable by DuckDB
(e.g. \code{"s3://obis-open-data/occurrence/*.parquet"}). Must expose columns
\code{decimalLongitude}, \code{decimalLatitude}, \code{species} and (optionally)
\code{aphiaid}, \code{phylum}, \code{class}, \code{order}, \code{family}, \code{genus}, \code{date_year},
\code{records}, \code{dropped}, \code{absence}. Missing taxonomic columns are filled NULL.}

\item{path_duckdb}{output DuckDB file path.}

\item{region_bbox}{optional \code{c(lon_min, lat_min, lon_max, lat_max)} to
restrict to a region (recommended for a first/demo build).}

\item{esn}{expected sample size for ES(n); default 50 (matches ES50).}

\item{s3_region}{AWS region for \code{s3://} sources (default \code{"us-east-1"}).}

\item{s3_anonymous}{use anonymous S3 access for public buckets (default TRUE).}

\item{overwrite}{overwrite an existing \code{path_duckdb} (default TRUE).}
}
\value{
\code{path_duckdb}, invisibly.
}
\description{
Reads OBIS occurrences, bins them to H3 cells, and writes an authoritative
DuckDB file with two layers consumed by the \code{h3t} tile service:
\itemize{
\item \code{idx_h3(res, cell_id, n, sp, shannon, simpson, es)} --- precomputed
all-taxa indicators for resolutions 1-7 (fast default tile layers).
\item \code{occ_h3(res, cell_id, aphiaid, phylum, class, "order", family, genus,
species, date_year, records)} --- species-level counts at resolution tiers
3/5/7 for on-the-fly taxon/year-filtered queries.
}
}
\details{
The indicator math (ES50, Shannon, Simpson, richness) is the SQL translation
of \code{\link[=calc_indicators]{calc_indicators()}} (\code{esn} = 50 by default), validated by the package
tests.
}
\concept{h3t}
