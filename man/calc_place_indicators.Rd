% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/place.R
\name{calc_place_indicators}
\alias{calc_place_indicators}
\title{Indicators rolled up from H3 cells to places}
\usage{
calc_place_indicators(
  con,
  places,
  name_col = NULL,
  res = 7L,
  eov = NULL,
  aphiaid = NULL,
  taxon = NULL,
  years = NULL,
  esn = 50L
)
}
\arguments{
\item{con}{connection from \code{\link[=obis_store_connect]{obis_store_connect()}}.}

\item{places}{an \code{sf} of polygons (lon/lat, EPSG:4326).}

\item{name_col}{column naming each place; default the first non-geometry
column.}

\item{res}{H3 resolution (1-7).}

\item{eov}{optional EOV name (one); see \code{\link[=obis_eov_seeds]{obis_eov_seeds()}}.}

\item{aphiaid}{optional WoRMS AphiaID(s) — the subtree filter.}

\item{taxon}{optional named list of DwC rank values; see \code{\link[=obis_h3t_sql]{obis_h3t_sql()}}.}

\item{years}{optional \code{c(min, max)} year range.}

\item{esn}{expected sample size for ES(n); default 50.}
}
\value{
data frame with \code{place}, \code{n_cells}, \code{n_cells_occupied}, and the
\code{\link[=calc_indicators]{calc_indicators()}} columns (\code{n}, \code{sp}, \code{shannon}, \code{simpson}, \code{es},
\code{hill_1}, \code{hill_2}, \code{hill_inf}).
}
\description{
Fills each polygon with H3 cells at \code{res}, sums species records over those
cells from \code{occ_h3} (optionally restricted to an EOV, an AphiaID subtree,
DwC rank values and/or a year range), and computes the indicators per place
with \code{\link[=calc_indicators]{calc_indicators()}}. Also reports the number of cells filled and the
number with any record, so sparse places are visible.
}
\concept{analyze}
