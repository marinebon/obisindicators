% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/store.R
\name{obis_bench_queries}
\alias{obis_bench_queries}
\title{The paper's default benchmark query set}
\usage{
obis_bench_queries(
  res = c(3L, 5L, 7L),
  eov = "fish",
  num_aphiaid = 137092L,
  den_aphiaid = 2688L,
  esn = 50L
)
}
\arguments{
\item{res}{resolutions to benchmark (default 3, 5, 7).}

\item{eov}{EOV for the precomputed/live comparison (default \code{"fish"}).}

\item{num_aphiaid, den_aphiaid}{SPUE target/effort AphiaIDs (default humpback
whale 137092 over Cetacea 2688).}

\item{esn}{expected sample size for ES(n).}
}
\value{
named character vector of SQL, ready for \code{\link[=obis_bench]{obis_bench()}}.
}
\description{
Builds the SQL for the four serving paths at each resolution: the
precomputed all-taxa layer (\code{idx_h3}), the precomputed EOV layer
(\code{idx_h3_eov}), the live EOV subtree over \code{occ_h3}, and the SPUE effort
proxy (two recursive subtrees).
}
\concept{store}
