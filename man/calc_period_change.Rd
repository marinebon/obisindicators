% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal.R
\name{calc_period_change}
\alias{calc_period_change}
\title{Change in an indicator between two periods, where both are reliable}
\usage{
calc_period_change(df, from, to, esn = 50L, indicator = c("es", "sp"))
}
\arguments{
\item{df}{output of \code{\link[=calc_period_indicators]{calc_period_indicators()}}.}

\item{from, to}{the two \code{period} values to compare.}

\item{esn}{reliability floor on \code{n} (default 50, the ES(50) floor).}

\item{indicator}{which indicator to difference (\code{"es"} or \code{"sp"}).}
}
\value{
list with \code{cells} (cell, value_from, value_to, delta, pct, n_from,
n_to) and \code{coverage} (counts of cells reliable in both / only from / only
to).
}
\description{
Joins the cells of two periods from \code{\link[=calc_period_indicators]{calc_period_indicators()}} and keeps
those with \code{n >= esn} in both, so ES(n) is defined on each side. Also
reports how coverage changed (cells reliable only in one period).
}
\concept{analyze}
