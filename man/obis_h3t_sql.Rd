% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/h3t.R
\name{obis_h3t_sql}
\alias{obis_h3t_sql}
\title{Build an h3t tile SQL query for an OBIS biodiversity indicator}
\usage{
obis_h3t_sql(
  indicator = c("es", "sp", "shannon", "n"),
  taxon = NULL,
  years = NULL,
  esn = 50L,
  res_placeholder = "{{res}}"
)
}
\arguments{
\item{indicator}{one of \code{"es"} (ES50), \code{"sp"} (richness), \code{"shannon"},
\code{"n"} (# records).}

\item{taxon}{optional named list/vector restricting taxa, names among
\code{phylum}, \code{class}, \code{order}, \code{family}, \code{genus}, \code{species}, e.g.
\code{list(class = "Aves")} or \code{list(phylum = c("Mollusca", "Cnidaria"))}.}

\item{years}{optional \code{c(min, max)} year range (either may be \code{NA}).}

\item{esn}{expected sample size for ES(n); default 50.}

\item{res_placeholder}{the resolution placeholder; default \code{"{{res}}"}.}
}
\value{
a single-line-friendly SQL string.
}
\description{
Generates the read-only \code{SELECT} (projecting exactly \code{cell_id, value, n})
that the \code{h3t} service base64-decodes, validates, and executes per tile.
\code{{{res}}} is substituted server-side with the tile's H3 resolution.
}
\details{
With no \code{taxon}/\code{years} filter the query reads the precomputed \code{idx_h3}
layer (fast). With a filter it computes the indicator on the fly from the
species-level \code{occ_h3} store, selecting the resolution tier that matches the
tile zoom.
}
\concept{h3t}
