% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taxon.R
\name{obis_taxon_children}
\alias{obis_taxon_children}
\title{Resolve the descendant taxa of a WoRMS AphiaID}
\usage{
obis_taxon_children(aphiaid, con)
}
\arguments{
\item{aphiaid}{one or more integer WoRMS AphiaID(s) (\code{taxonID}) to root the
subtree at.}

\item{con}{a \code{DBI} connection to a DuckDB store exposing a \code{taxon} table with
columns \code{taxonID}, \code{parentNameUsageID}, \code{acceptedNameUsageID},
\code{scientificName}, \code{taxonRank}.}
}
\value{
data frame with one row per taxon in the subtree: \code{taxonID},
\code{parentNameUsageID}, \code{acceptedNameUsageID}, \code{scientificName}, \code{taxonRank},
and \code{depth_level} (0 for the seed, incremented per generation).
}
\description{
Recursively walks the WoRMS \code{taxon} table (baked into the obis_h3 DuckDB
store) down \code{parentNameUsageID} to return the seed taxon plus every
descendant, at any rank. This is the local-snapshot replacement for the
heavy OBIS \verb{taxonid=} service queries: given e.g. Infraorder Cetacea's
AphiaID it returns all its species, whose AphiaIDs then filter \code{occ_h3}.
}
\concept{taxon}
