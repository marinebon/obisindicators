% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/store.R
\name{obis_cell_indicators}
\alias{obis_cell_indicators}
\title{Per-cell indicators for a filter at one resolution}
\usage{
obis_cell_indicators(
  con,
  res,
  eov = NULL,
  aphiaid = NULL,
  taxon = NULL,
  years = NULL,
  esn = 50L,
  live = NULL
)
}
\arguments{
\item{con}{connection from \code{\link[=obis_store_connect]{obis_store_connect()}}.}

\item{res}{H3 resolution (1-7).}

\item{eov}{optional EOV name (one); see \code{\link[=obis_eov_seeds]{obis_eov_seeds()}}.}

\item{aphiaid}{optional WoRMS AphiaID(s) — the subtree filter.}

\item{taxon}{optional named list of DwC rank values; see \code{\link[=obis_h3t_sql]{obis_h3t_sql()}}.}

\item{years}{optional \code{c(min, max)} year range.}

\item{esn}{expected sample size for ES(n); default 50.}

\item{live}{force the live \code{occ_h3} path even when a precomputed layer
exists (default NULL = use the precomputed layer when possible).}
}
\value{
data frame with \code{cell}, \code{n}, \code{sp}, \code{shannon}, \code{es} (and \code{simpson}
when read from a precomputed layer).
}
\description{
Returns every indicator the tile builders expose — record count \code{n},
richness \code{sp}, \code{shannon}, ES(n) \code{es} — for one filter (all taxa, an EOV, an
AphiaID subtree, or DwC rank values) at one H3 resolution, as a data frame
keyed on the hex string. Reads a precomputed layer (\code{idx_h3}, \code{idx_h3_eov})
in one query when the filter allows, otherwise runs the live tile SQL once
per indicator and joins. No new indicator math lives here: everything comes
from \code{\link[=obis_h3t_sql]{obis_h3t_sql()}} / \code{\link[=obis_eov_sql]{obis_eov_sql()}}, which are pinned to
\code{\link[=calc_indicators]{calc_indicators()}} by the package tests.
}
\concept{analyze}
