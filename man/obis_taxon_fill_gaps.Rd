% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taxon_gapfill.R
\name{obis_taxon_fill_gaps}
\alias{obis_taxon_fill_gaps}
\title{Fill gaps in the \code{taxon} table from the WoRMS REST API}
\usage{
obis_taxon_fill_gaps(
  con,
  fetch = wm_aphia_records,
  max_rounds = 40L,
  min_records = 0L,
  verbose = TRUE
)
}
\arguments{
\item{con}{a \code{DBI} connection to a \strong{writable} store with \code{occ_h3} + \code{taxon}.}

\item{fetch}{function taking an integer vector of AphiaIDs and returning a
data frame shaped like \code{\link[=wm_aphia_records]{wm_aphia_records()}}'s output; the seam that lets
tests run without network.}

\item{max_rounds}{runaway guard on closure rounds. Each round climbs exactly
one generation, and a full WoRMS chain (every intermediate rank from species
to Biota) runs ~15-20 deep, so this needs headroom well past the depth of
the \emph{named} ranks. If the guard is hit while ancestors are still missing the
fill warns and reports \code{closed = FALSE} rather than leaving you to believe
the tree is whole.}

\item{min_records}{only chase orphans with at least this many records.}

\item{verbose}{message per-round progress.}
}
\value{
invisibly, a list with \code{added} (data frame of inserted rows),
\code{unresolved} (AphiaIDs WoRMS had no record for), \code{rounds},
\code{records_recovered} (occurrence records made reachable by the fill), and
\code{closed} (did the ancestor walk reach closure within \code{max_rounds}).
}
\description{
Supplements the bulk WoRMS \code{taxon.txt} download with per-id lookups for every
AphiaID that \code{occ_h3} carries but \code{taxon} lacks, then keeps going until the
tree is \emph{closed}: each round also fetches any ancestor newly referenced by the
rows just inserted. Without that closure an orphan species stays disconnected
from its seed and remains unreachable by \code{\link[=obis_taxon_children]{obis_taxon_children()}}.
}
\details{
Writes to \code{con}, so open the store read-write (see
\code{data-raw/migrate_fill_taxon_gaps.R}, which copies first).
}
\concept{taxon}
