% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taxon.R
\name{obis_spue_sql}
\alias{obis_spue_sql}
\title{Build an h3t tile SQL query for the SPUE effort proxy}
\usage{
obis_spue_sql(
  num_aphiaid,
  den_aphiaid,
  res_max = 7L,
  res_placeholder = "{{res}}",
  bbox_placeholder = "{{bbox}}"
)
}
\arguments{
\item{num_aphiaid}{target-taxon AphiaID(s) (numerator subtree).}

\item{den_aphiaid}{effort-taxon AphiaID(s) (denominator subtree); typically a
higher-order taxon such as the target's parent class.}

\item{res_max}{cap on H3 resolution (1-7); see \code{\link[=obis_h3t_sql]{obis_h3t_sql()}}.}

\item{res_placeholder}{resolution placeholder; default \code{"{{res}}"}.}

\item{bbox_placeholder}{spatial-prune placeholder spliced into the \code{occ_h3}
scan; default \code{"{{bbox}}"} (the \code{h3t} server substitutes a per-tile
\code{lat}/\code{lng} predicate). Pass \code{""} to disable for direct execution — see
\code{\link[=obis_h3t_sql]{obis_h3t_sql()}}.}
}
\value{
a SQL string.
}
\description{
Live per-cell \verb{value = records(target subtree) / records(effort subtree)},
restricted to the effort taxon's footprint. Both subtrees are resolved with
recursive CTEs over the baked \code{taxon} table, so this works for arbitrary
ranks. Projects exactly \verb{cell_id, value, n} (with \code{n} = effort record count),
as the \code{h3t} service requires. See \code{\link[=calc_spue]{calc_spue()}} for the pinned R reference.
}
\concept{taxon}
