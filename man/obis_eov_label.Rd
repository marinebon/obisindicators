% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eov.R
\name{obis_eov_label}
\alias{obis_eov_label}
\title{Human-readable EOV label with its taxonomic definition}
\usage{
obis_eov_label(eov = NULL, max_taxa = 3L)
}
\arguments{
\item{eov}{EOV name(s); see \code{\link[=obis_eov_seeds]{obis_eov_seeds()}}.}

\item{max_taxa}{list seed names up to this many, then summarise as a count.}
}
\value{
character vector, one label per EOV, named by EOV.
}
\description{
A compact "what is this, taxonomically" string for pickers and legends, e.g.
\code{"Sea turtles (superfamily Chelonioidea)"}. Lives in the package rather than
the consuming app so a label can never drift from the seeds it describes.
}
\details{
One seed renders as \verb{rank Name}; a few render as a name list; many collapse to
a count, since a picker cannot usefully show 19 mangrove genera.
}
\examples{
obis_eov_label()
}
\concept{eov}
