% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/store.R
\name{obis_bench}
\alias{obis_bench}
\title{Benchmark queries against a store}
\usage{
obis_bench(con, queries, reps = 3L)
}
\arguments{
\item{con}{connection from \code{\link[=obis_store_connect]{obis_store_connect()}}.}

\item{queries}{named character vector of SQL statements (no \code{{{res}}}
placeholders — bind them first, e.g. via \code{res_placeholder}).}

\item{reps}{number of warm repetitions after the cold run (default 3).}
}
\value{
data frame with \code{label}, \code{rows}, \code{cold_s}, \code{warm_s} (median).
}
\description{
Times each SQL statement: the first (cold) run and the median of \code{reps}
further (warm) runs, plus the number of rows returned. Use with
\code{\link[=obis_bench_queries]{obis_bench_queries()}} for the paper's default set, or any named character
vector of SQL.
}
\concept{store}
