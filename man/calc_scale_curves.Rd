% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale.R
\name{calc_scale_curves}
\alias{calc_scale_curves}
\title{Scale curves: indicator summaries across H3 resolutions}
\usage{
calc_scale_curves(
  con,
  res = 1:7,
  eov = NULL,
  aphiaid = NULL,
  taxon = NULL,
  years = NULL,
  esn = 50L,
  floors = c(10L, 30L, 100L),
  group = NULL
)
}
\arguments{
\item{con}{connection from \code{\link[=obis_store_connect]{obis_store_connect()}}.}

\item{res}{resolutions to evaluate (default 1:7).}

\item{eov}{optional EOV name (one); see \code{\link[=obis_eov_seeds]{obis_eov_seeds()}}.}

\item{aphiaid}{optional WoRMS AphiaID(s) — the subtree filter.}

\item{taxon}{optional named list of DwC rank values; see \code{\link[=obis_h3t_sql]{obis_h3t_sql()}}.}

\item{years}{optional \code{c(min, max)} year range.}

\item{esn}{expected sample size for ES(n); default 50.}

\item{floors}{record-count floors for the reliability columns
\verb{frac_n_ge_<floor>} (default 10, 30, 100).}

\item{group}{optional label carried in a \code{group} column (handy when
binding curves for several EOVs).}
}
\value{
data frame, one row per resolution.
}
\description{
For one filter (all taxa, an EOV, an AphiaID subtree or DwC rank values),
summarizes the per-cell indicators at each resolution: occupied cells,
records, the distribution of records per cell, the fraction of cells where
ES(n) is defined (\code{n >= esn}), and medians of ES(n) and richness among
those. \code{n_cells_all} is the number of cells occupied by \emph{any} taxon at that
resolution (from \code{idx_h3}), so \code{frac_cells_all} reads as the filter's
footprint relative to all sampling.
}
\concept{analyze}
