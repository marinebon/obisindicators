% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/store.R
\name{obis_store_connect}
\alias{obis_store_connect}
\title{Connect to an obis_h3 DuckDB store}
\usage{
obis_store_connect(
  path = Sys.getenv("OBIS_H3_DUCKDB"),
  read_only = TRUE,
  install_h3 = TRUE
)
}
\arguments{
\item{path}{path to the \code{.duckdb} file; defaults to the \code{OBIS_H3_DUCKDB}
environment variable (set it once per machine, e.g. in \verb{~/.Renviron}).}

\item{read_only}{open read-only (default TRUE; the analysis functions only
read). Pass FALSE to bake layers (e.g. \code{\link[=obis_eov_bake]{obis_eov_bake()}}).}

\item{install_h3}{run \verb{INSTALL h3 FROM community} before \verb{LOAD h3} (needs
network the first time; default TRUE).}
}
\value{
a \code{DBI} connection. Disconnect with
\code{DBI::dbDisconnect(con, shutdown = TRUE)}.
}
\description{
Opens a (by default read-only) DuckDB connection to a store produced by
\code{\link[=build_obis_h3_duckdb]{build_obis_h3_duckdb()}} and loads the \code{h3} community extension, which every
query in this package needs (\code{h3_cell_to_parent}, \code{h3_h3_to_string}, ...).
}
\concept{store}
