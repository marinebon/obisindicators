% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eov.R
\name{obis_eov_sql}
\alias{obis_eov_sql}
\title{Build an h3t tile SQL query for an Essential Ocean Variable}
\usage{
obis_eov_sql(
  eov,
  indicator = c("es", "sp", "shannon", "n"),
  years = NULL,
  esn = 50L,
  res_max = 7L,
  res_placeholder = "{{res}}",
  live = NULL
)
}
\arguments{
\item{eov}{EOV name(s); see \code{\link[=obis_eov_seeds]{obis_eov_seeds()}}. Multiple names always take
the live path (the precomputed layer stores one EOV per row).}

\item{indicator}{one of \code{"es"} (ES50), \code{"sp"}, \code{"shannon"}, \code{"n"}.}

\item{years}{optional \code{c(min, max)} year range; forces the live path.}

\item{esn}{expected sample size for ES(n); default 50.}

\item{res_max}{cap on H3 resolution (1-7).}

\item{res_placeholder}{resolution placeholder; default \code{"{{res}}"}.}

\item{live}{force the live \code{occ_h3} path (e.g. against a store where
\code{\link[=obis_eov_bake]{obis_eov_bake()}} has not been run). Default: only when it must.}
}
\value{
a SQL string.
}
\description{
Routes to the precomputed \code{idx_h3_eov} layer when it can (no year filter,
one EOV — as fast as the all-taxa \code{idx_h3} path), otherwise falls back to a
live aggregation over \code{occ_h3} filtered to the EOV's AphiaID subtree via
\code{\link[=obis_h3t_sql]{obis_h3t_sql()}}. Projects exactly \verb{cell_id, value, n}.
}
\examples{
obis_eov_sql("seaTurtles")
obis_eov_sql("fish", indicator = "n", years = c(2000, 2020))
}
\concept{eov}
