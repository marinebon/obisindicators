% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualize.R
\name{gmap_cells}
\alias{gmap_cells}
\title{Map a per-cell table of indicators}
\usage{
gmap_cells(
  df,
  column = "es",
  label = column,
  trans = "identity",
  mask = NULL,
  ...
)
}
\arguments{
\item{df}{data frame with a \code{cell} column (e.g. from
\code{\link[=obis_cell_indicators]{obis_cell_indicators()}}, \code{\link[=calc_spue_cells]{calc_spue_cells()}}).}

\item{column}{column to fill by (default \code{"es"}).}

\item{label}{legend label (default \code{column}).}

\item{trans}{scale transformation (see \code{\link[=gmap_indicator]{gmap_indicator()}}).}

\item{mask}{optional logical vector (same length as \code{df}) — cells where
\code{FALSE} are drawn with \code{NA} fill (e.g. \code{df$n >= 50} to mask unreliable
ES(50)).}

\item{...}{passed to \code{\link[=gmap_indicator]{gmap_indicator()}} (\code{crs}, \code{bbox}).}
}
\value{
ggplot2 plot
}
\description{
Joins a data frame keyed on \code{cell} (hex string) to hexagon polygons and
draws it with \code{\link[=gmap_indicator]{gmap_indicator()}}.
}
\concept{visualize}
