% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{h3_raster_to_cells}
\alias{h3_raster_to_cells}
\title{Aggregate a raster (e.g. an SDM) to H3 cells}
\usage{
h3_raster_to_cells(
  r,
  res,
  cells = NULL,
  method = c("auto", "centers", "centroids"),
  ratio = 3
)
}
\arguments{
\item{r}{a \code{terra::SpatRaster} (single layer, lon/lat).}

\item{res}{H3 resolution.}

\item{cells}{optional hex strings to restrict/sample to (required for
\code{"centroids"}).}

\item{method}{\code{"auto"}, \code{"centers"} or \code{"centroids"}.}

\item{ratio}{hexagon-to-raster-cell area ratio above which \code{"auto"} uses
centres (default 3).}
}
\value{
data frame with \code{cell}, \code{value} (mean), \code{n_px} (raster cells
averaged; 1 for centroid sampling).
}
\description{
Two strategies: \code{"centers"} assigns every non-NA raster cell centre to an
H3 cell at \code{res} and averages (right when hexagons are larger than raster
cells); \code{"centroids"} samples the raster at each requested hexagon's
centroid (right when hexagons are smaller). \code{"auto"} picks centres when the
mean hexagon area is at least \code{ratio} times the raster cell area.
}
\concept{analyze}
