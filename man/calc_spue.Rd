% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taxon.R
\name{calc_spue}
\alias{calc_spue}
\title{Sightings-per-unit-effort (SPUE) effort proxy, per H3 cell (R reference)}
\usage{
calc_spue(df, num_aphiaid, den_aphiaid)
}
\arguments{
\item{df}{data frame of species-level records with columns \code{cell},
\code{aphiaid}, \code{records} (as in \code{occ_h3}).}

\item{num_aphiaid}{integer AphiaIDs of the \strong{target} subtree (already
resolved via \code{\link[=obis_taxon_children]{obis_taxon_children()}}).}

\item{den_aphiaid}{integer AphiaIDs of the \strong{effort} subtree (denominator).}
}
\value{
data frame with \code{cell}, \code{n_num}, \code{n_den}, \code{spue}; only cells with
effort records (\code{n_den > 0}) are returned.
}
\description{
Presence-only effort proxy: within the spatial footprint of a higher-order
"effort" taxon (e.g. all Cetacea, from multi-species surveys), the fraction
of records that are the target taxon. \code{spue = n_num / n_den} where the
denominator is every record of the effort subtree in the cell and the
numerator is the subset that are the target subtree. This is the pure-R
reference pinned to \code{\link[=obis_spue_sql]{obis_spue_sql()}} by the package tests.
}
\concept{taxon}
