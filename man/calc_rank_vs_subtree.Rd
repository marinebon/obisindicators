% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{calc_rank_vs_subtree}
\alias{calc_rank_vs_subtree}
\title{Records and species by DwC rank column vs by AphiaID subtree}
\usage{
calc_rank_vs_subtree(con, presets = obis_rank_presets(), res = H3T_RES_BASE)
}
\arguments{
\item{con}{connection from \code{\link[=obis_store_connect]{obis_store_connect()}}.}

\item{presets}{data frame with \code{label}, \code{rank}, \code{name}, \code{aphiaid}; default
\code{\link[=obis_rank_presets]{obis_rank_presets()}}.}

\item{res}{resolution tier of \code{occ_h3} to count on (default the base, 7).}
}
\value{
data frame with \code{label}, \code{rank}, \code{name}, \code{aphiaid}, \code{records_rank},
\code{species_rank}, \code{records_tree}, \code{species_tree}, \code{ratio}
(\code{records_rank / records_tree}).
}
\description{
For each preset, counts records and species at the base resolution two
ways: filtering the Darwin Core rank column (\code{"<rank>" = '<name>'}) and
filtering \code{aphiaid} by the recursive WoRMS subtree of the seed id. Where
WoRMS files the name at a rank other than the column (e.g. Actinopterygii is
a gigaclass; OBIS's \code{class} carries Teleostei), the rank column silently
returns zero while the subtree finds the records.
}
\concept{analyze}
